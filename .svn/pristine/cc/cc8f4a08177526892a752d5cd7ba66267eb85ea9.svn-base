// 全局 目录配置(输出: 导航菜单 / 路由)
import { API_DIR, THRIFT_DIR } from './config'; // 路由
const globalDir = [
  {
    id: '1',
    title: '引导页',
    key: '1',
    path: '/home/guide',
    icon: 'flag',
    component: require('../containers/home/homeIndex').default,
  },
  API_DIR,
  THRIFT_DIR,
];
console.log('API_DIR-----', API_DIR);
console.log('THRIFT_DIR-----', THRIFT_DIR);
console.log('globalDir-----', globalDir);
export default globalDir;
